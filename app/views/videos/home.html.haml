%article.video_category
  %header
    %h3= link_to "Series"
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to video, nil do
          %img(src="#{video.thumbnail_url}")

